# originals-threecard — "Three Card Poker" verifier

Open-source, MIT reference implementation that lets **anyone** reproduce and verify a settled
**Three Card Poker** bet on [maczo Originals](https://verify.maczo.co/originals/) — offline, from the
`serverSeed` / `clientSeed` / `nonce` alone.

This repo is one game in the Originals verifier fleet. The cryptographic core (HMAC-SHA256 → `uint32`
stream, per `libs/provably_fair/fair.py`) lives ONCE in the shared engine
[`@maczo/originals-verify`](https://github.com/maczo-co/originals-verify); this repo adds only the pure
`Three Card Poker` **resolver** and its published odds.

| | |
|---|---|
| 🧮 Verify a bet | <https://verify.maczo.co/originals/?game=threecard> |
| 📖 Docs | <https://verify.maczo.co/originals/docs.html#threecard> |
| ⚖️ Published odds | <https://verify.maczo.co/originals/paytables/threecard.json> |
| 🔧 Shared engine | <https://github.com/maczo-co/originals-verify> |

- **Bias class:** `uniform` — maps words without a biased modulo reduction (bit extraction, or a uniform Fisher-Yates / without-replacement draw) — no modulo bias.
- **Recipe:** deck = shuffle(52); player = [0..2], dealer = [3..5]; 3-card ranks (SF>trips>straight>flush>pair>high). Play or fold; settle ante/play + ante bonus. multiplier_e8 = total return per ante.

## Files

- `resolve.js` — the pure resolver: `resolve(uints, params, paytable, { rtpE8, betMinor })` → `{ multiplierE8, win, payoutMinor, outcome }`. Mirrors `libs/game_math/threecard.py`.
- `paytable.json` — the published odds + the exact `e8` tables the resolver reads (computed by the server at RTP 99%).
- `test-vectors.json` — known-answer vectors generated by the authoritative Python.
- `verify.js` — the self-test CLI.

## Verify a settled bet yourself

```
npm install
npm test        # reproduces every known-answer vector (JS ⇄ Python)
```

To check one real bet, deep-link into the shared verifier with your bet's
`server` (revealed seed), `commit`, `client`, `nonce` and `params` — see the docs link above.

## License

MIT © 2026 maczo
